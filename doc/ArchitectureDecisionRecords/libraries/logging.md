* programmatic logging configuration: dev/test different from default-prod; provide mechanism to override(?), maybe even
  runtime, to "debug" = increase log; in running prod
* former "mu kotlin"; with lambdas instead String.format (or string concatenation => performance)
