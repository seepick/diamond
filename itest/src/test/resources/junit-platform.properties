# yes, this one works
cucumber.execution.parallel.enabled=true
# this number will be multiplied with the number of cores available (default = 1)
cucumber.execution.parallel.config.dynamic.factor=1
# no, those are not necessary (it seems? working with junit THROUGH cucumber... i guess)
#junit.jupiter.execution.parallel.enabled = true
#junit.jupiter.execution.parallel.mode.default = concurrent
#junit.jupiter.execution.parallel.mode.classes.default = concurrent
