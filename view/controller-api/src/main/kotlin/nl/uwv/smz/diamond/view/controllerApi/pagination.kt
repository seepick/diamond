package nl.uwv.smz.diamond.view.controllerApi

object QueryParamNames {
    object Pagination {
        /** aka `offset` */
        const val SKIP = "skip"

        /** aka `limit` */
        const val TAKE = "take"
    }

    /** aka `orderBy` */
    const val SORT = "sort"
}
